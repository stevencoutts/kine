# Not included directly. Anchors are duplicated per fragment because
# Compose `include:` does not share anchors across files. Kept here as
# the canonical reference for what every app fragment should look like.
#
# x-app: &app
#   restart: unless-stopped
#   environment:
#     PUID: ${PUID}
#     PGID: ${PGID}
#     UMASK: ${UMASK}
#     TZ: ${KINE_TIMEZONE}
#   networks: [kine_internal, kine_edge]
#   security_opt: [no-new-privileges:true]
