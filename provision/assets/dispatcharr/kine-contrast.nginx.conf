# Inject kine contrast CSS into Dispatcharr HTML (tv. hostname).
# Helm embed already inlines the same rules via embed_proxy.
sub_filter '</head>' '<link rel="stylesheet" href="/static/kine-contrast.css?v=2" data-kine-dispatcharr-contrast></head>';
sub_filter_once on;
